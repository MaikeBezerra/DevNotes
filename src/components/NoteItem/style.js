import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    noteContainer: {
        padding: 15,
        borderStyle: "solid",
        borderBottomColor: '#222',
        borderBottomWidth: 1
    },
    textNote: {
        color: "#FFF",
        fontSize: 17
    }
})